                  .map(p => <option key={p.id} value={p.name}>{p.name}</option>)
                }
              </select>
              <select name="governorate" value={filters.governorate} onChange={handleFilterChange} className="px-3 py-2 border rounded-lg text-sm">
                <option value="">جميع المحافظات</option>
                {filterGovernorates.map(g => <option key={g.id || g.name} value={g.name}>{g.name}</option>)}
              </select>
              <input type="text" name="request_number" value={filters.request_number} onChange={handleFilterChange} placeholder="رقم الطلب" className="px-3 py-2 border rounded-lg text-sm" />
              <input type="text" name="ccb_report_number" value={filters.ccb_report_number} onChange={handleFilterChange} placeholder="رقم CCP" className="px-3 py-2 border rounded-lg text-sm" />
              <select name="request_status" value={filters.request_status} onChange={handleFilterChange} className="px-3 py-2 border rounded-lg text-sm">
